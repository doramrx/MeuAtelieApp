import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1.2,
  },

  emptyListContainer: {
    alignItems: "center",
    justifyContent: "center",
  },

  emptyListText: {
    marginTop: 20,
    fontSize: 18,
    textAlign: "center",
  },
});
